/* eslint-disable max-len */
const path = require("path");

module.exports = {
    favicon: path.resolve("src", "images", "favicon.png"), // Used for manifest favicon generation
    shortName: "HasThemes", // shortname for manifest. *Must* be shorter than 12 characters
    themeColor: "#5E60E7",
    backgroundColor: "#fff",
};
